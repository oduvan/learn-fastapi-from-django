name: Deploy MkDocs site to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install MkDocs Material + i18n plugin
        run: pip install "mkdocs-material>=9,<10" "mkdocs-static-i18n"

      - name: Build site
        run: mkdocs build --strict

      - uses: actions/configure-pages@v5
        with:
          # Enable GitHub Pages (Actions build type) on first run so the
          # deploy doesn't fail with "Get Pages site failed / Not Found".
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
